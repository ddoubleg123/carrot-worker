FROM scratch AS disabled

# This Dockerfile is intentionally disabled. The project uses the root-level
# Dockerfile for builds/deploys. This stub exists only to satisfy editor linters
# that require a valid FROM in every Dockerfile. No build artefacts are emitted
# from this stage.
