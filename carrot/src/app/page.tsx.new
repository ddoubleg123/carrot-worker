import { redirect } from 'next/navigation';

export default function Home() {
  // Redirect to the new v2 homepage
  redirect('/(versions)/homepage-v2');
  
  // This return is a fallback and won't be reached due to the redirect
  return null;
}
